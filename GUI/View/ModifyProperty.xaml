<UserControl x:Class="GUI.View.ModifyProperty"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalonDock="http://avalondock.codeplex.com"
             mc:Ignorable="d" 
              xmlns:vm="clr-namespace:GUI.ViewModel"
             xmlns:v="clr-namespace:GUI.View"
             d:DesignHeight="400" d:DesignWidth="150">

    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible"
                      VerticalAlignment="Center" HorizontalAlignment="Stretch"
                      Style="{StaticResource ScrollViewerStyle}">

        <Grid Background="White" Visibility="{Binding DialogVisbility}" Height="248" Width="179" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
  
            <DataGrid Grid.Row="0" Grid.Column="0" Name="ShowFeatureProperty" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
                       AutoGenerateColumns="True" />
        </Grid>
    </ScrollViewer>

</UserControl>
